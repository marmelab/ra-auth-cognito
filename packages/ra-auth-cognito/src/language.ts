export type CognitoTranslationMessages = {
    ra: {
        auth: {
            cognito: {
                mfa: {
                    totp: {
                        association_helper_text: string;
                        association_label: string;
                        association_required: string;
                        copy_totp_url_to_clipboard: string;
                        label: string;
                    };
                };
                require_new_password: string;
            };
        };
    };
};
